with(obj_thornstalk){
tsize = random_range(0.5,1.8);
image_xscale = choose(tsize*-1,tsize);
depth = 20;
}
with(obj_titletree){
var size = random_range(0.5,2);
image_xscale = size;
image_yscale = size;
depth = 20;
}